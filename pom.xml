<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>poc.exploit</groupId>
    <artifactId>poc-exploit-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>poc-exploit</name>
    <description>POC CQRS-like : outbox Namastack, Kafka, projecteurs, OpenTelemetry</description>

    <modules>
        <module>event-contract</module>
        <module>gateway</module>
        <module>order-service</module>
        <module>order-projector</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <spring-cloud.version>2025.1.3</spring-cloud.version>
        <namastack-outbox.version>1.9.0</namastack-outbox.version>
        <datasource-micrometer.version>2.3.0</datasource-micrometer.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.namastack</groupId>
                <artifactId>namastack-outbox-bom</artifactId>
                <version>${namastack-outbox.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Requêtes SQL en spans (Micrometer Observation), Apache 2.0 -->
            <dependency>
                <groupId>net.ttddyy.observation</groupId>
                <artifactId>datasource-micrometer-spring-boot</artifactId>
                <version>${datasource-micrometer.version}</version>
            </dependency>
            <dependency>
                <groupId>poc.exploit</groupId>
                <artifactId>event-contract</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
